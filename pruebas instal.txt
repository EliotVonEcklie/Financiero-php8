**********Crear Tabla para Tipo de Radicacón***********
CREATE TABLE IF NOT EXISTS `plantiporadicacion` (
  `codigo` int(11) NOT NULL,
  `nombre` varchar(100) NOT NULL,
  `descripcion` varchar(100) NOT NULL,
  `dias` int(11) NOT NULL,
  `tdias` varchar(2) NOT NULL,
  `slectura` varchar(1) NOT NULL,
  `adjunto` varchar(1) NOT NULL,
  `estado` varchar(1) NOT NULL
) ENGINE=MyISAM DEFAULT CHARSET=latin1;


******Adicinar Campos para Cargo, consulta e Historial en la Tabla de Responsable de Tarea******
ALTER TABLE  `planacresponsables` ADD  `codcargo` INT NOT NULL
ALTER TABLE  `planacresponsables` ADD  `tipot` BOOL NOT NULL COMMENT  'interno o externo';
ALTER TABLE  `planacresponsables` ADD  `consulta` VARCHAR( 400 ) NOT NULL 
ALTER TABLE  `planacresponsables` ADD  `idhistory` INT( 11) NOT NULL
ALTER TABLE  `planacresponsables` ADD  `horasig` TIME NOT NULL ,
ADD  `horresp` TIME NOT NULL

******Adicinar Campos para tipo y Estados2 en la Tabla de Radicacion******
ALTER TABLE  `planacradicacion` ADD  `tipot` BOOL NOT NULL COMMENT  'interno o externo';
ALTER TABLE  `planacradicacion` ADD  `estado2` INT( 1 ) NOT NULL

****Insertar Ruta para el reporte de tareas*****
INSERT INTO `opciones` (`id_opcion`, `nom_opcion`, `ruta_opcion`, `niv_opcion`, `est_opcion`, `orden`, `modulo`, `especial`, `comando`) VALUES
(NULL, 'Tareas Dependencias', 'plan-tareasdependencias.php', 4, '1', 9, 9, '', '');

****Quitar auto numerico a planacradicacion*****
ALTER TABLE  `planacradicacion` CHANGE  `numeror`  `numeror` INT( 11 ) NOT NULL COMMENT  'numero de radicación';

****Campo Nuevo para el Tipo de Tarea Interna o Externa*****
ALTER TABLE  `planacarchivosad` ADD  `tipot` BOOL NOT NULL COMMENT  'interno o externo';


****Pasar Cargo a la tabla de Responsables****

UPDATE planacresponsables TB1 SET TB1.codcargo=(SELECT TB2.codcargo FROM planestructura_terceros TB2  WHERE TB1.usuariocon = TB2.cedulanit)

SELECT TB2.codcargo FROM planacresponsables TB1,planestructura_terceros TB2,  WHERE TB1.usuariocon = TB2.cedulanit 

UPDATE planacradicacion SET estado='AN' WHERE estado='A';
SELECT estado planacradicacion WHERE estado<>'A';

